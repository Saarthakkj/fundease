import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
// Remove or comment out the next line if index.css is not used or missing
// import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

// The following lines should be removed or commented out if reportWebVitals is not used
// import reportWebVitals from './reportWebVitals';
// reportWebVitals();
